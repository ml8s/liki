# Archived skill-up assets

These cases and recorded outputs target the former JSON-RPC / `agent_cli.py` skill surface.
They are retained as evaluation history and are not part of `make gate`.

Current deterministic MCP contracts live in:

```bash
make test-counsel
make test-engine-mcp
```
