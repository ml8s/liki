#!/bin/sh
# pre-push — 推送前执行检查（lint + vet + unit + 方法名），失败则阻断推送
# 由 core.hooksPath=.githooks 生效；勿在 .git/hooks/ 重复配置钩子

set -e

echo "==> pre-push: 正在执行 make pre-push ..."
cd "$(dirname "$0")/.."
make pre-push
echo "==> pre-push: 检查通过，推送继续"
