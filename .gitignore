# Reasonix agent config
.git-old/
.reasonix/
reasonix.toml

# Local IDE config (any level — root and engine/.claude)
.claude/

# Engine built binary (top-level make build)
bin/

# OS
.DS_Store
Thumbs.db
dist/

# 评测产物（agent 输出——可再生；skill-up 默认输出在 skill 目录旁）
tests/liki-workspace/
# Python 缓存
__pycache__/
*.pyc

# 评测临时配置（run-qwen.sh 生成的临时 yaml）
.run-eval.*.yaml

# 数据检查产物（可重新生成）
tests/RESULTS.md
tests/evals/*.log
