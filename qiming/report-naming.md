# 起名报告模板

基于对应 API 返回数据生成。字段含义参考 `rpc.discover` 返回的 schema。只引用实际存在的字段，不编造。

## 报告结构

### 一、用神

一句话：日主强弱、身强身弱、用神喜忌 → 起名宜补五行方向。

### 二、候选名字

每个名字格式：**名字** — 一句解释（整体寓意 + 五行匹配 + 风格定位，融合说清）。考虑三才的路径在末尾括号注明（三才吉·五格吉），不考虑的路径不注。

### 三、推荐

一句话给出综合推荐。
