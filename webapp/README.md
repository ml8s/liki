# webapp/ — liki-web 部署负载（非本仓运行内容）

liki-web 报告流水线（`mingshu/` 命书、`hepan/` 合盘）的 prompt 卡源文件，部署时
复制进 liki-bazi skill 副本的 `webapp/` 目录。

文内 `liki-bazi/webapp/...` 引用是 **liki-web 挂载点的部署相对路径**，在本仓库内
不可解析——不是死链，请勿在本仓"修复"这些路径。
